{% if cart.items %}
<div class="outer_container">
  <h1>{{ section.settings.heading }}</h1>
  <p>{{ cart.items.price }}</p>

  
  <div class="block_container">
    {% for block in section.blocks %}
      <div class="block_content">
         <img src="{{ block.settings.block_image | img_url:'large'}}" />
         <h2> {{ block.settings.blocktext }}</h2>
        <button onclick="hi()">Add to cart</button>
      </div>
     
    {% endfor %}
  </div>

  
  
</div>
<button onclick="showw()">Show/Hide</button>

{% endif %}

<style>
  block_content img{
    height:80px;
    width:80px;
  }
</style>


{% schema %}
  {
    "name": "Try",
    "settings": [
      {
        "type":"text",
        "id":"heading",
        "label":"Main Heading",
        "default":"Try heading"
      }
    ],
    "blocks":[
      {
        "type":"image",
        "name":"Custom block",
        "settings":[
          {
            "type":"image_picker",
            "id":"block_image",
            "label":"Enter item image"
          },
          {
            "type":"text",
            "id":"blocktext",
            "label":"Item description",
            "default":"Enter Item Description"
          }
        ]
      }
    ],
    "presets":[{
      "category":"Image",
      "name":"Try section"
    }]
  }
{% endschema %}
<script>
function hi(){ console.log('Hi!')
              let text = document.getElementById("block_container")
              
              
                console.log(text)
             }
function showw(){
  $(".block_container").toggle()
  
  
  
}
 
</script>

<style>
  .block_container{
    display:flex;
  }
  .block_content{
    margin:15px;
  }
</style>